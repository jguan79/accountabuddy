// Model for each task. Will hold the values below.
export interface Task {
    id: string;
    subjectTitle: string;
    dueDate: string;
    color: string;
    description: string;
    userId: string;
}
